#Postgresql Configuration
spring.datasource.url=jdbc:postgresql://dpg-d78bcobuibrs73bk0nl0-a.singapore-postgres.render.com/moneymanager_az36
spring.datasource.username=moneymanager_az36_user
spring.datasource.password=Xv65N1oeSX4xZUrFrINN4Df80L410L5z


#application-prod.properties


# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false

# Email Configuration - Using Port 465 (SSL) for Render compatibility
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=465
spring.mail.username=${BREVO_USERNAME}
spring.mail.password=${BREVO_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp-relay.brevo.com
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from=${BREVO_FROM_EMAIL}

# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# Frontend URL
budgetbee.frontend.url=${MONEY_MANAGER_FRONTEND_URL}

# Backend URL for activation links
app.activation.url=${MONEY_MANAGER_BACKEND_URL}